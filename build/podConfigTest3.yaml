apiVersion : v1
kind: Pod
metadata :
  name : nginx-1
  labels:
    app: nginx
    env: prod
spec:
  containers :
    - name : nginx
      image : nginx:1.14.2
      resources:
        limits:
          cpu: 10m
          memory: 32M
      volumeMounts:
        - name: html
          mountPath: /data/media
      ports:
        - containerPort: 70
    - name : nginx
      image : nginx:1.14.2
      resources:
        limits:
          cpu: 10m
          memory: 32M
      volumeMounts:
        - name: html
          mountPath: /data/media
      ports:
        - containerPort: 70
  volumes:
    - name: html
      emptyDir: {}

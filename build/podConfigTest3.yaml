apiVersion : v1
kind: Pod
metadata :
  name : podtest
  labels:
    app: podtest
    env: prod
spec:
  containers :
    - name : podtest-1
      image : saltfishy/httpserver:v4
      resources:
        limits:
          cpu: 10m
          memory: 32M
      volumeMounts:
        - name: html
          mountPath: /data/media
      ports:
        - containerPort: 8888
      command: [ "/bin/sh","-c","./main" ]
    - name : podtest-2
      image : nginx:1.14.2
      resources:
        limits:
          cpu: 10m
          memory: 32M
      volumeMounts:
        - name: html
          mountPath: /data/media
      ports:
        - containerPort: 70
  volumes:
    - name: html
      emptyDir: {}
